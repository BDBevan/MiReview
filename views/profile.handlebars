<article class="centered profile" data-theme="dark">
    <header>
        <hgroup>
            <h2>User Profile</h2>
        </hgroup>
    </header>

    <main class="left-aligned">
        <h4>Name: {{ user_name }}</h4>
        <h4>Email: {{ user_email }}</h4>
        <h4>Review Count: </h4>
    </main>
</article>